import { DRE } from "@/features/financeiro/relatorios/DRE";

/** Casca de rota (F7 §7.3) — a página real vive em features/financeiro/relatorios. */
export default function DREPage() {
  return <DRE />;
}
